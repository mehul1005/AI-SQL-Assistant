<Window x:Class="AiSqlAssistant.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AI SQL Assistant" Height="600" Width="900" WindowStartupLocation="CenterScreen" Background="#F3F4F6">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Database Schema Context:" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,5"/>
        <TextBox x:Name="SchemaTextBox" Grid.Row="1" Height="80" TextWrapping="Wrap" AcceptsReturn="True" 
                 FontFamily="Consolas" Padding="10" Margin="0,0,0,15"
                 Text="Table: Applications (Id INT, AppName VARCHAR, CustomGroup VARCHAR, CreatedDate DATETIME)"/>

        <Grid Grid.Row="2" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="PromptTextBox" Grid.Column="0" Height="50" TextWrapping="Wrap" AcceptsReturn="True" 
                     Padding="10" FontSize="14" Margin="0,0,15,0"
                     Text="Show me all applications assigned to the custom group PC, ordered by newest."/>

            <Button x:Name="GenerateButton" Grid.Column="1" Content="Generate SQL" Width="140" Height="50" 
                    Background="#0078D4" Foreground="White" FontWeight="Bold" FontSize="14"
                    Click="GenerateButton_Click"/>
        </Grid>

        <Border Grid.Row="3" Background="#1E1E1E" CornerRadius="5" Padding="2">
            <TextBox x:Name="OutputTextBox" Background="Transparent" Foreground="#D4D4D4" FontFamily="Consolas" 
                     FontSize="15" IsReadOnly="True" TextWrapping="Wrap" BorderThickness="0" Padding="15"
                     Text="-- Generated SQL will appear here..."/>
        </Border>
    </Grid>
</Window>